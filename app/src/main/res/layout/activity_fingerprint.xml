<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/margin_horizontal_fragment_title"
        android:layout_marginRight="@dimen/margin_horizontal_fragment_title"
        android:layout_marginTop="@dimen/margin_top_fragment_title"
        android:gravity="center"
        android:text="@string/fingerprint_required"
        android:textSize="@dimen/text_size_fragment_title"
        android:textStyle="bold"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/fingerprint"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginBottom="@dimen/margin_fragment_image"
        android:layout_marginLeft="@dimen/margin_fragment_image"
        android:layout_marginRight="@dimen/margin_fragment_image"
        android:layout_marginTop="@dimen/margin_fragment_image"
        app:layout_constraintBottom_toTopOf="@+id/description"
        app:layout_constraintDimensionRatio="1:1"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/title" />

    <TextView
        android:id="@+id/description"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/margin_bottom_fragment"
        android:layout_marginLeft="@dimen/margin_horizontal_fragment_description"
        android:layout_marginRight="@dimen/margin_horizontal_fragment_description"
        android:gravity="center"
        android:maxEms="18"
        android:text="@string/fingerprint_required_description"
        android:textSize="@dimen/text_size_fragment_description"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent" />

</android.support.constraint.ConstraintLayout>